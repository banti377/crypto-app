import React, { ReactElement } from 'react';

function Card(): ReactElement {
  return (
    <div className="flex items-center justify-center shadow-xl rounded-md bg-gray-200 h-72 w-80">
      test
    </div>
  );
}

export default Card;
